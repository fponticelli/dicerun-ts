/**
 * Enumeration that tries to capture different kind of entities that are
 * normally found has decoder inputs.
 */
export declare enum Entity {
    CHARACTER = 0,
    CHARACTER_CODE = 1,
    DIGIT = 2,
    LETTER = 3,
    LITERAL = 4,
    LOWER_CASE_LETTER = 5,
    PREDICATE = 6,
    UPPERCASE_LETTER = 7,
    STRING = 8,
    STRING_INSENSITIVE = 9,
    TYPE = 10,
    WHITESPACE = 11
}
/**
 * Make `Entity` into english words accounting for pluralization by using `qt`.
 */
export declare const entityToString: (entity: Entity, qt: number) => "string" | "literal" | "character" | "character code" | "predicate" | "letter" | "uppercase letter" | "lowercase letter" | "digit" | "string insensitive" | "type" | "whitespace" | "characters" | "character codes" | "predicates" | "letters" | "literals" | "uppercase letters" | "lowercase letters" | "digits" | "strings" | "strings insensitive" | "types" | "whitespaces";
