export const RR = {
    getResult(result) {
        if (result.type === 'one-result') {
            return result.die.result;
        }
        else {
            return result.result;
        }
    }
};
